**Sharper answers.** Twelve automated quality cycles since RC3, each one blind-judged and re-verified:
- Local questions stay local — "coffee shops near williamsburg" means Brooklyn, not Virginia
- Open-now verdicts use the real clock, tested down to 2 AM ("closed at midnight, reopens at noon today")
- Weather has a second live source and will never invent a forecast when the feed is down
- "What's playing" names real current movies instead of sending you elsewhere
- Funnel recommendations are real things that exist, honor every option you click, and never invent a feature or a train route to force a fit

**Cleaner app.**
- New in Manage models: an auto-cleanup switch that removes superseded model versions once their replacement is installed (frees multiple GB on most setups)
- The version number lives quietly under the system monitor now; the wordmark, menu bar, and titlebar are properly matched and centered
- Answers speak as ConcordeAI — no model machinery names in your chat
- Fixed: after an update, the app could keep showing a cached older page
